import org.kde.kirigami 2.15 as Kirigami

/**
 * @inherit Kirigami.ShadowedRectangle
 * @brief Just a wrapper for the Kirigami type.
 * 
 * @bug This is meant to be eventually forked. For a free MauiKit version of kirigami as dependency.
 */
Kirigami.ShadowedRectangle
{

}
